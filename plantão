<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema de Escala - SESAB/CGFP</title>
    <style>
        :root {
            /* Paleta de cores atualizada */
            --preto: #000000;
            --vermelho: #E30613;
            --azul: #034991;
            --amarelo: #FCBE00;
            --verde-limao: #95C11F;
            --laranja: #EF7D00;
            --verde-agua: #31B7BC;
            
            --light-bg: #f8f9fa;
            --text: #333;
            --border-radius: 4px;
            --box-shadow: 0 2px 6px rgba(0,0,0,0.1);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--light-bg);
            color: var(--text);
            line-height: 1.6;
        }

        .header {
            background-color: var(--preto);
            padding: 15px 0;
            border-bottom: 3px solid var(--vermelho);
        }

        .logo-container {
            display: flex;
            align-items: center;
            justify-content: center;
            max-width: 1200px;
            margin: 0 auto;
        }

        .logo {
            height: 60px;
            margin-right: 20px;
        }

        .title-container {
            text-align: center;
        }

        .title {
            color: white;
            font-size: 24px;
            margin-bottom: 5px;
        }

        .subtitle {
            color: var(--amarelo);
            font-size: 14px;
        }

        .main-container {
            max-width: 1200px;
            margin: 30px auto;
            padding: 0 20px;
            display: grid;
            grid-template-columns: 300px 1fr;
            gap: 20px;
        }

        .panel {
            background: white;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            padding: 20px;
        }

        .panel-title {
            color: var(--azul);
            border-bottom: 1px solid var(--verde-agua);
            padding-bottom: 10px;
            margin-bottom: 15px;
            font-size: 18px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: var(--azul);
            font-size: 14px;
        }

        input, select {
            width: 100%;
            padding: 8px 10px;
            border: 1px solid #ddd;
            border-radius: var(--border-radius);
        }

        .btn {
            padding: 10px;
            border: none;
            border-radius: var(--border-radius);
            cursor: pointer;
            width: 100%;
            margin-bottom: 10px;
            font-weight: 500;
        }

        .btn-primary {
            background-color: var(--vermelho);
            color: white;
        }

        .btn-secondary {
            background-color: var(--azul);
            color: white;
        }

        .calendar {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        .calendar th {
            background-color: var(--preto);
            color: white;
            padding: 10px;
            text-align: center;
        }

        .calendar td {
            border: 1px solid #eee;
            padding: 10px;
            height: 80px;
            vertical-align: top;
        }

        .day-number {
            font-weight: bold;
            margin-bottom: 5px;
        }

        @media (max-width: 768px) {
            .main-container {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="logo-container">
            <img class="logo" src="c27ad07c-7ca6-468a-8896-e201b4992507" alt="Logo SESAB">
            <div class="title-container">
                <h1 class="title">Sistema de Escala de Plantões</h1>
                <p class="subtitle">Secretaria da Saúde do Estado da Bahia - CGFP</p>
            </div>
        </div>
    </header>

    <main class="main-container">
        <div class="panel">
            <h2 class="panel-title">Configuração</h2>
            <div class="form-group">
                <label for="month">Mês de referência:</label>
                <input type="month" id="month">
            </div>
            <div class="form-group">
                <label for="participant">Adicionar participante:</label>
                <input type="text" id="participant" placeholder="Nome completo">
            </div>
            <button class="btn btn-primary">Adicionar</button>
            <button class="btn btn-secondary">Gerar Escala</button>
        </div>

        <div class="panel">
            <h2 class="panel-title">Escala do Mês</h2>
            <table class="calendar">
                <thead>
                    <tr>
                        <th>Dom</th>
                        <th>Seg</th>
                        <th>Ter</th>
                        <th>Qua</th>
                        <th>Qui</th>
                        <th>Sex</th>
                        <th>Sáb</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- O calendário será gerado dinamicamente via JavaScript -->
                </tbody>
            </table>
        </div>
    </main>

    <script>
        // Aqui viria o JavaScript para funcionalidades do sistema
        document.addEventListener('DOMContentLoaded', function() {
            console.log('Sistema carregado');
            // Implementar lógica de geração de escala aqui
        });
    </script>
</body>
</html>
